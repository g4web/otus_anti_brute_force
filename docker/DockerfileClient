FROM golang:1.18.5

ENV GOPATH=/

COPY .. ./

RUN go mod download

RUN CGO_ENABLED=0 GOOS=linux go build -a -installsuffix cgo -o ./abf-client ./api/client/

CMD exec /bin/bash -c "trap : TERM INT; sleep infinity & wait"
